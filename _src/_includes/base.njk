<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="author" content="{{ meta.authorURL }}">
        <meta name="description" content="{{ meta.description }}">

        <link rel="stylesheet" href="/css/style.css">

        <title>{{pageTitle}}</title>
    </head>
    <body>
        {% include 'header.njk' %}
        {% if logo == true -%}
        {{ content | safe }}
        {% else %}
        <main {% if maxWidth == true -%} class="maxWidth" {% endif %}>
            {{ content | safe }}
        </main>
        {% endif %}
        {% include 'footer.njk' %}
    </body>
</html>